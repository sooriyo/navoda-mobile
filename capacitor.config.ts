import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'navoda.fusion.lumiraq.com',
  appName: 'Navoda Sales',
  webDir: 'dist/navoda/browser',

};

export default config;
